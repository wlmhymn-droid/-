package §1F§
{
   import battlePanic.character.§-S§;
   import battlePanic.character.CharacterFactory;
   
   public class §['§ extends §-S§
   {
       
      
      public function §['§()
      {
         var _loc1_:Boolean = true;
         var _loc2_:Boolean = false;
         if(!(_loc2_ && _loc1_))
         {
            super();
            while(true)
            {
               syncToDefinition(CharacterFactory.§6Q§);
               addr51:
               if(_loc1_ || _loc1_)
               {
                  color = shared.RED;
                  addr58:
                  if(!_loc1_)
                  {
                     while(!(_loc2_ && Boolean(this)))
                     {
                        §§goto(addr51);
                        §§goto(addr58);
                     }
                     while(true)
                     {
                        §%4§ = 15;
                        §§goto(addr44);
                     }
                     addr44:
                     addr71:
                  }
                  return;
               }
            }
         }
         §§goto(addr71);
      }
   }
}
