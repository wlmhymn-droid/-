package §4'§
{
   import battlePanic.character.CharacterFactory;
   
   public class §%;§ extends §"M§
   {
       
      
      public function §%;§()
      {
         var _loc1_:Boolean = false;
         var _loc2_:Boolean = true;
         if(!(_loc1_ && _loc2_))
         {
            super();
            loop0:
            while(true)
            {
               syncToDefinition(CharacterFactory.§1E§);
               while(true)
               {
                  §%4§ = 5;
                  while(!(_loc1_ && Boolean(this)))
                  {
                     if(!_loc1_)
                     {
                        §;[§ = 10;
                        if(_loc2_ || Boolean(this))
                        {
                           return;
                           addr44:
                        }
                        continue;
                        continue;
                     }
                     continue loop0;
                  }
               }
            }
         }
         §§goto(addr44);
      }
   }
}
